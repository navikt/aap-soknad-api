
no.nav:
  security:
    jwt:
      client:
        registration:
          pdl-api:
            token-exchange:
              audience: someaudience
            well-known-url:  http://localhost:${mock-oauth2-server.port}/issuer1/.well-known/openid-configuration
            grant-type: urn:ietf:params:oauth:grant-type:token-exchange
            authentication:
              client-id: id
              client-jwk: dummy
              client-auth-method: private_key_jwt
      issuer:
        idporten:
          discovery-url: http://localhost:${mock-oauth2-server.port}/issuer1/.well-known/openid-configuration
          accepted-audience: someaudience
pdl:
  base-uri: https://pdl-api.dev-fss-pub.nais.io/graphql