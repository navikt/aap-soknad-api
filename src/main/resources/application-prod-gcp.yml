management:
    endpoints:
        web:
            exposure:
                include: '*'
    endpoint:
        health:
            show-details: always
no.nav:
    security:
        jwt:
            client:
                registration:
                    aareg-services:
                        token-exchange:
                            audience: ${env}-fss:arbeidsforhold:aareg-services-nais
                        well-known-url: ${token.x.well.known.url}
                        grant-type: urn:ietf:params:oauth:grant-type:token-exchange
                        authentication:
                            client-id: ${token.x.client.id}
                            client-jwk: ${token.x.private.jwk}
                            client-auth-method: private_key_jwt

                    safselvbetjening:
                        token-exchange:
                            audience: ${env}-fss:teamdokumenthandtering:safselvbetjening
                        well-known-url: ${token.x.well.known.url}
                        grant-type: urn:ietf:params:oauth:grant-type:token-exchange
                        authentication:
                            client-id: ${token.x.client.id}
                            client-jwk: ${token.x.private.jwk}
                            client-auth-method: private_key_jwt

arbeidsforhold:
    base-uri: https://aareg-services.${env}-fss-pub.nais.io
organisasjon:
    base-uri: https://ereg-services.${env}-fss-fss-pub.nais.io
saf:
    base-uri: https://safselvbetjening.${env}-fss-pub.nais.io
konto:
    enabled: false
vl:
    standard:
        enabled: false
    ettersending:
        enabled: false
    utland:
        enabled: false
joark:
    scope: api://${env}-fss.teamdokumenthandtering.dokarkiv/.default
minside:
    backlinks:
        innsyn: https://aap-innsyn.intern.nav.no/aap/innsyn
        standard: https://aap-soknad.intern.nav.no/aap/soknad/standard
        utland: https://aap-soknad.intern.nav.no/aap/soknad/utland